# Production Environment Variables
# These values are used when building for production (pnpm build)

# API URL - Production backend endpoint
NEXT_PUBLIC_API_URL=https://api.n9n.archcode.space

# WebSocket URL - Production WebSocket endpoint (uses wss:// for secure connection)
NEXT_PUBLIC_WS_URL=wss://api.n9n.archcode.space
